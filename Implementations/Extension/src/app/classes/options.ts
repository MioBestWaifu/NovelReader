//This model (if not explicitly) is used as the options object throughout the entire extension, not only in the Angular part. 
export class Options{
    trackPages!:boolean;
    translateSelection!:boolean;
}